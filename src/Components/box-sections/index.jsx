import './style.css'

const BoxSection = ({title}) => (
    <div className="boxSection">
        <p className="Title TitleSection">{title}</p>
        <a href=""> Veja todos</a>
    </div>
)

export default BoxSection